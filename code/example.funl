

let true
	fun (x y) x
	
let false
	fun (x y) y
	
let if
	fun (b then else)
		(b then else)
		
let and
	fun (b1 b2)
		(if b1 b2 false)
		
let or
	fun (b1 b2)
		(if b1 true b2)
		
let not
	fun (b)
		(if b false true)
		
(or (and true false)
	(or false (not false)) 
		
		
		